# Status on 2026-09-23

## What is already true

- PR #6 is merged.
- PR #6's merge commit is `7bc8d71e76d2b75028f923dc7655ab245d4d0cf8`.
- PR #1's exact head is the same `7bc8d71e76d2b75028f923dc7655ab245d4d0cf8`.
- `arm-thumb-test-suite` also points at `7bc8d71e76d2b75028f923dc7655ab245d4d0cf8`.
- The latest workflow run observed for that exact PR #1 head passed.
- Therefore there is no separate #1-vs-#6 merge to perform: the code is already combined.

## Why PR #1 should not merge into current main

Current `main` is the direct-DEX sibling line. The repository now explicitly treats DEX and ARM/Thumb as separate backend lines. Pulling the old native ARM branch into DEX `main` merely to close an ancient PR would destroy that separation.

## Remaining problem

The real native-ARM cleanup problem is to reconcile the two useful descendants of the old test-suite checkpoint:

- `arm-thumb-test-suite` — numerical backend + character specifications + first executable PrintASCII path;
- `branching-dispatch-fixtures` — later branching fixtures and machine/device oracles, including framebuffer, speaker, and GPIO work.

That reconciliation should happen on an explicitly native ARM branch, with the DEX line remaining separate.

The follow-up issue created with this archive owns that topology problem. This directory preserves #1 + #6 so the old PR wrapper can be closed without losing either code or rationale.
